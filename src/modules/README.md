# Modules

Each business module will contain `domain`, `application`, and `infrastructure` folders when warranted. Do not add empty abstraction layers; establish them with the first tested use case. Modules communicate through explicit application interfaces or versioned domain events.
